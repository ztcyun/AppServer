upstream  service_api {
  server $SERVER_API_PORT;
}

upstream  service_api_system {
  server $SERVER_API_SYSTEM_PORT;
}


upstream  service_urlshortener {
  server $SERVER_URLSHORYENER_PORT;
}


upstream  service_studio.notify {
  server $SERVER_STUDIO_NOTIFY_PORT;
}


upstream  service_people.server {
  server $SERVER_PEOPLE_SERVER_PORT;
}


upstream  service_files {
  server $SERVER_FILES_PORT;
}


upstream  service_files_services {
  server $SERVER_FILES_SERVICES_PORT;
}


upstream  service_studio {
  server $SERVER_STUDIO_PORT;
}